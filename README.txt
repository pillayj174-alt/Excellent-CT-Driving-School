Excellent CT Driving School — Hero Removed

Replace the repository's existing index.html with the included index.html.

Changes:
- Removed the complete <section id="home" class="hero">...</section>.
- Removed the hero visual, dashboard, road scene, vehicle, hero text, buttons and hero contact block.
- Kept the trust strip and all other website sections.
- Changed the header/footer brand links from #home to #about so they no longer point to a removed section.
- No other business information or functionality was intentionally changed.

Note: The existing styles.css still contains unused hero CSS. It is harmless, but can be cleaned up separately once you confirm the new layout.
